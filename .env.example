# ============================================
# Crew Zingy — Variables de Entorno (Ejemplo)
# Copia este archivo como .env y rellena tus datos reales
# ============================================

# --- Base de Datos (MySQL) ---
MYSQL_ROOT_PASSWORD=TuPasswordRootSegura!
MYSQL_DATABASE=crew_zingy_db
MYSQL_USER=crew_zingy_app
MYSQL_PASSWORD=TuPasswordApp123!
MYSQL_PORT=3307
DATABASE_URL=mysql://crew_zingy_app:TuPasswordApp123!@localhost:3307/crew_zingy_db

# --- Adminer (Gestor visual de BD) ---
ADMINER_PORT=8081

# --- Puerto externo en Docker (la app corre en 3000 internamente) ---
APP_PORT=3001

# --- NextAuth (Sesión de Admin) ---
# Generar con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
NEXTAUTH_SECRET=reemplaza_con_clave_aleatoria_muy_larga
NEXTAUTH_URL=https://tudominio.com

# --- JWT de Clientes (Sesión de PWA) ---
# Generar con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
CLIENT_JWT_SECRET=reemplaza_con_otra_clave_aleatoria_muy_larga

# --- Aplicacion ---
NEXT_PUBLIC_BASE_URL=https://tudominio.com
UPLOAD_PATH=./public/uploads

# --- Typebot (Configurable desde Admin > Integraciones) ---
NEXT_PUBLIC_TYPEBOT_ID=tu-typebot-id
NEXT_PUBLIC_TYPEBOT_API_HOST=https://typebot.tudominio.com

# --- Web Push (VAPID Keys) ---
# Generar par de claves: npx web-push generate-vapid-keys
NEXT_PUBLIC_VAPID_PUBLIC_KEY="tu_clave_vapid_publica_aqui"
VAPID_PRIVATE_KEY="tu_clave_vapid_privada_aqui"
VAPID_SUBJECT="mailto:contacto@tudominio.com"

# --- Seed (Solo para primer despliegue) ---
# Al correr el sistema por primera vez, crea el admin con estas credenciales
INITIAL_ADMIN_EMAIL=admin@tucomercio.com
INITIAL_ADMIN_PASSWORD=CambiarEstaContrasena123!

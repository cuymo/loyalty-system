# ============================================
# 🚀 CREW ZINGY — VARIABLES DE ENTORNO
# ============================================
# Instrucciones para DOKPLOY: 
# 1. Ve a la pestaña "Environment" de tu aplicación en Dokploy.
# 2. Copia todo este archivo y pégalo ahí.
# 3. ¡IMPORTANTE! Las variables que empiezan con "NEXT_PUBLIC_" deben estar definidas 
#    ANTES de darle al botón de "Deploy" para que React las compile.
# ============================================

# --------------------------------------------
# 🌐 1. DOMINIOS Y RUTAS (CRÍTICO PARA DOKPLOY)
# --------------------------------------------
# Tu dominio en producción (Ej: https://crew.zingystore.com)
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# URL que usa NextAuth para callbacks
NEXTAUTH_URL=http://localhost:3000
# Ruta para subida de archivos (déjalo por defecto)
UPLOAD_PATH=./public/uploads

# --------------------------------------------
# 🔒 2. SEGURIDAD Y JWT (CAMBIAR SIEMPRE EN PRODUCCIÓN)
# --------------------------------------------
# Genera claves seguras con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

# Para cifrar la sesión de los Administradores (NextAuth)
NEXTAUTH_SECRET=cambiar_por_clave_super_secreta_en_produccion

# Para cifrar la sesión de los Clientes (App/PWA)
CLIENT_JWT_SECRET=cambiar_por_otra_clave_secreta_distinta_en_prod

# --------------------------------------------
# 🗄️ 3. BASE DE DATOS (POSTGRESQL)
# --------------------------------------------
# Credenciales del contenedor Postgres que creaste en Dokploy
POSTGRES_USER=crew_zingy_app
POSTGRES_PASSWORD=TuPasswordApp123!
POSTGRES_DB=crew_zingy_db

# URL de Conexión (El host en Dokploy suele ser el nombre del contenedor de BD interno)
DATABASE_URL=postgresql://crew_zingy_app:TuPasswordApp123!@localhost:5432/crew_zingy_db

# --------------------------------------------
# 🔑 4. PRIMER USUARIO ADMIN (SEED RUNTIME)
# --------------------------------------------
# IMPORTANTE: El backend no arrancará si esto está vacío.
# Solo se usa la primera vez para crear la cuenta maestra.
INITIAL_ADMIN_EMAIL=admin@miempresa.com
INITIAL_ADMIN_PASSWORD=ContrasenaSuperSegura123!

# --------------------------------------------
# 🤖 5. INTEGRACIONES EXTERNAS (OPCIONAL EN DEV, REQUERIDO PARA FUNCIONES)
# --------------------------------------------
# Typebot (Flujos conversacionales de WhatsApp)
NEXT_PUBLIC_TYPEBOT_API_HOST=https://typebot.tudominio.com
NEXT_PUBLIC_TYPEBOT_ID=id_del_bot_aqui

# Web Push Notifications (Generar con: npx web-push generate-vapid-keys)
NEXT_PUBLIC_VAPID_PUBLIC_KEY="generar_y_pegar_aqui"
VAPID_PRIVATE_KEY="generar_y_pegar_aqui"
VAPID_SUBJECT="mailto:soporte@miempresa.com"
